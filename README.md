# minimal-dotfiles

vimとzshの最低限の設定  
主に新規サーバーのセットアップ時に使用する

# File
- .vimrc
- .zshrc

# Install

```
$ sudo yum install zsh vim
```
  
```
$ sudo apt-get install zsh vim
```

シェルの変更

```
chsh -s $(which zsh)
```
